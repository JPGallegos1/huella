# Supabase — proyecto "huella"
# Modelo de claves nuevo: publishable key (sb_publishable_...) reemplaza a la
# anon legacy; secret key (sb_secret_...) reemplaza a service_role.

# --- Frontend (apps/chat, apps/app) ---
# Vite expone solo variables con prefijo VITE_. La publishable key es PÚBLICA
# (se envía al navegador) y respeta RLS por organización.
VITE_SUPABASE_URL=https://hyenpnjwdgutqkibmglr.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable__e1_2PFXVZL0WzfvMBYjAQ_-d4RQRUf

# --- Backend (apps/api) ---
SUPABASE_URL=https://hyenpnjwdgutqkibmglr.supabase.co
# secret key: SECRETA, bypassa RLS. NUNCA la expongas en el cliente ni la commitees.
# Obtenela en: Dashboard → Project Settings → API Keys → Secret keys
SUPABASE_SECRET_KEY=
