# Supabase — proyecto "huella"
# URL pública del proyecto (segura de exponer)
SUPABASE_URL=https://hyenpnjwdgutqkibmglr.supabase.co

# --- Frontend (apps/chat, apps/app) ---
# Vite expone solo variables con prefijo VITE_. Usá la anon/publishable key
# (es pública: respeta RLS por organización). Obtenela en:
# Supabase Dashboard → Project Settings → API
VITE_SUPABASE_URL=https://hyenpnjwdgutqkibmglr.supabase.co
VITE_SUPABASE_ANON_KEY=

# --- Backend (apps/api) ---
# service_role key: SECRETA, bypassa RLS. NUNCA la expongas en el cliente.
SUPABASE_SERVICE_ROLE_KEY=
